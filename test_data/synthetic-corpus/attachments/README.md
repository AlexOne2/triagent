# Attachment Notes

The generated corpus embeds attachment payloads directly inside the `.eml`
samples in `../samples/`.

This directory is reserved for future reusable attachment source templates if
the corpus grows beyond the current code-generated payload set.
